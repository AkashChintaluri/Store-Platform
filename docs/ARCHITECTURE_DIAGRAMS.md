# Store Platform Architecture Diagrams

## Type Erasure Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                         API Request                              │
│                  POST /api/stores                                │
│                  { name: "store", engine: "woocommerce" }        │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      routes/stores.py                            │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ 1. generate_values(name, host, engine)                     │ │
│  │ 2. install_store(name, namespace, values_file)             │ │
│  │ 3. configure_platform(namespace, name, engine)             │ │
│  │ 4. get_store_password(namespace, name, engine)             │ │
│  └────────────────────────────────────────────────────────────┘ │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                  orchestrator/provisioner.py                     │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ adapter = get_store_adapter(engine)  # Type Erasure!       │ │
│  │ return adapter.configure_platform(...)                     │ │
│  └────────────────────────────────────────────────────────────┘ │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    adapters/factory.py                           │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ def get_store_adapter(engine: StoreEngine) -> StoreAdapter │ │
│  │     adapters = {                                           │ │
│  │         "woocommerce": WooCommerceAdapter,                 │ │
│  │         "medusa": MedusaAdapter,                           │ │
│  │     }                                                       │ │
│  │     return adapters[engine]()  # Returns interface type    │ │
│  └────────────────────────────────────────────────────────────┘ │
└────────────────────────────┬────────────────────────────────────┘
                             │
                ┌────────────┴────────────┐
                │                         │
                ▼                         ▼
┌──────────────────────────┐  ┌──────────────────────────┐
│ WooCommerceAdapter       │  │ MedusaAdapter            │
│ (Concrete Implementation)│  │ (Placeholder)            │
├──────────────────────────┤  ├──────────────────────────┤
│ + configure_platform()   │  │ + configure_platform()   │
│   - Install WooCommerce  │  │   - NotImplementedError  │
│   - Create test product  │  │                          │
│   - Enable COD payment   │  │ TODO: Implement          │
│                          │  │ - Run migrations         │
│ + get_admin_password()   │  │ - Create admin user      │
│   - Read K8s secret      │  │ - Seed data              │
│                          │  │                          │
│ + get_store_url_path()   │  │ + get_store_url_path()   │
│   - Returns "/shop/"     │  │   - Returns "/"          │
└──────────────────────────┘  └──────────────────────────┘
```

## Component Interaction

```
┌─────────────┐
│   Client    │  User creates store via UI
│  (React)    │
└──────┬──────┘
       │ HTTP POST
       ▼
┌─────────────────────────────────────────────────────────┐
│                    FastAPI Backend                      │
│                                                         │
│  ┌──────────────┐      ┌─────────────────────────┐    │
│  │   Routes     │──────▶│   Orchestrator          │    │
│  │              │      │                         │    │
│  │ - stores.py  │      │ - provisioner.py        │    │
│  │              │      │ - status.py             │    │
│  └──────────────┘      └──────────┬──────────────┘    │
│                                   │                    │
│                                   │ Uses               │
│                                   ▼                    │
│                        ┌────────────────────┐          │
│                        │  Adapter System    │          │
│                        │                    │          │
│                        │ ┌────────────────┐ │          │
│                        │ │ StoreAdapter   │ │          │
│                        │ │  (Interface)   │ │          │
│                        │ └────────────────┘ │          │
│                        │         ▲          │          │
│                        │         │          │          │
│                        │    Implements      │          │
│                        │         │          │          │
│                        │ ┌───────┴────────┐ │          │
│                        │ │  WooCommerce   │ │          │
│                        │ │    Medusa      │ │          │
│                        │ │   (Future...)  │ │          │
│                        │ └────────────────┘ │          │
│                        └────────────────────┘          │
└─────────────────────────────┬───────────────────────────┘
                              │
                              │ Helm Commands
                              ▼
                    ┌──────────────────┐
                    │   Kubernetes     │
                    │                  │
                    │  ┌────────────┐  │
                    │  │ Namespace  │  │
                    │  │  store-1   │  │
                    │  │            │  │
                    │  │ - WordPress│  │
                    │  │ - MariaDB  │  │
                    │  │ - Ingress  │  │
                    │  └────────────┘  │
                    └──────────────────┘
```

## Adapter Pattern Detail

```
┌──────────────────────────────────────────────────────────────┐
│                     StoreAdapter (ABC)                       │
│                                                              │
│  Abstract Methods (Contract):                               │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ + get_chart_dependency() -> dict                       │ │
│  │ + get_default_values(name, host) -> dict               │ │
│  │ + configure_platform(namespace, release) -> tuple      │ │
│  │ + get_admin_password(namespace, release) -> str        │ │
│  │ + get_pod_selector(release) -> str                     │ │
│  │ + get_store_url_path() -> str                          │ │
│  │ + is_platform_ready(namespace, release) -> bool        │ │
│  └────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
                            ▲
                            │ Inherits
                            │
         ┌──────────────────┼──────────────────┐
         │                  │                  │
         │                  │                  │
┌────────┴────────┐ ┌───────┴────────┐ ┌──────┴──────┐
│ WooCommerce     │ │    Medusa      │ │   Future    │
│   Adapter       │ │   Adapter      │ │  Adapters   │
├─────────────────┤ ├────────────────┤ ├─────────────┤
│ ✅ Implemented  │ │ 🚧 Placeholder │ │ 📝 Template │
│                 │ │                │ │   Ready     │
│ WordPress +     │ │ Node.js +      │ │             │
│ WooCommerce     │ │ PostgreSQL     │ │ Shopify?    │
│ plugin          │ │                │ │ Magento?    │
│                 │ │ TODO:          │ │ PrestaShop? │
│ Bitnami chart   │ │ - Migrations   │ │             │
│ MariaDB         │ │ - Admin user   │ │             │
│ /shop/ path     │ │ - Seed data    │ │             │
└─────────────────┘ └────────────────┘ └─────────────┘
```

## Before vs After Refactoring

### Before (Tightly Coupled)
```
┌─────────────────────────────────────────┐
│         provisioner.py                  │
│                                         │
│  def configure_woocommerce():           │
│      # Hardcoded WooCommerce logic      │
│      pod = get_wordpress_pod()          │
│      install_woocommerce_plugin()       │
│      create_test_product()              │
│      enable_cod_payment()               │
│                                         │
│  # To add Medusa:                       │
│  # - Add configure_medusa()             │
│  # - Add if/else in routes              │
│  # - Duplicate similar code             │
│  # - Hard to maintain!                  │
└─────────────────────────────────────────┘
```

### After (Type Erasure)
```
┌─────────────────────────────────────────┐
│         provisioner.py                  │
│                                         │
│  def configure_platform(engine):        │
│      adapter = get_store_adapter(engine)│
│      return adapter.configure_platform()│
│                                         │
│  # To add Medusa:                       │
│  # 1. Create MedusaAdapter              │
│  # 2. Register in factory               │
│  # 3. Done! No changes here!            │
│  # ✅ Open/Closed Principle             │
└─────────────────────────────────────────┘
```

## Data Flow Example

```
User Action: Create WooCommerce Store
│
├─ 1. Frontend sends: { name: "mystore", engine: "woocommerce" }
│
├─ 2. API Route receives request
│   └─ Validates input with Pydantic model
│
├─ 3. Generate Helm values
│   ├─ adapter = get_store_adapter("woocommerce")
│   ├─ values = adapter.get_default_values("mystore", "mystore.example.com")
│   └─ Returns: { wordpress: {...}, mariadb: {...} }
│
├─ 4. Install Helm chart
│   └─ helm upgrade --install mystore ./charts/store -f values.yaml
│
├─ 5. Wait for pods ready
│   └─ kubectl get pods -n mystore
│
├─ 6. Configure platform
│   ├─ adapter = get_store_adapter("woocommerce")
│   ├─ success, error = adapter.configure_platform("mystore", "mystore")
│   └─ Executes:
│       ├─ Install WooCommerce plugin
│       ├─ Create test product
│       └─ Enable COD payment
│
├─ 7. Get admin password
│   ├─ adapter = get_store_adapter("woocommerce")
│   └─ password = adapter.get_admin_password("mystore", "mystore")
│
└─ 8. Return response
    └─ { id: "...", status: "READY", url: "http://mystore.example.com/shop/" }
```

## Extension Point

```
Adding a New Platform (e.g., Shopify)

Step 1: Create Adapter
┌─────────────────────────────────────┐
│  adapters/shopify.py                │
│                                     │
│  class ShopifyAdapter(StoreAdapter):│
│      def configure_platform(...):   │
│          # Shopify-specific setup   │
│          pass                       │
└─────────────────────────────────────┘

Step 2: Register in Factory
┌─────────────────────────────────────┐
│  adapters/factory.py                │
│                                     │
│  adapters = {                       │
│      "woocommerce": WooCommerce..., │
│      "medusa": MedusaAdapter,       │
│      "shopify": ShopifyAdapter, ←── │
│  }                                  │
└─────────────────────────────────────┘

Step 3: Update Type
┌─────────────────────────────────────┐
│  models.py                          │
│                                     │
│  engine: Literal[                   │
│      "woocommerce",                 │
│      "medusa",                      │
│      "shopify"  ←──                 │
│  ]                                  │
└─────────────────────────────────────┘

✅ Done! No changes to orchestrator or routes needed!
```
